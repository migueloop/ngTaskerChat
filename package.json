{
  "name": "ngtaskerchat",
  "private": false,
  "version": "0.0.2",
  "description": "A starter project for AngularJS",
   "repository": {
     "type": "git",
     "url": "https://github.com/migueloop/ngTaskerChat"
  },
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",
    /*"start:dev": "./node_modules/.bin/live-server --port=9000",*/
    "start" : "http-server -a localhost -p 8000 -c-1",
    "postinstall": "bower install"
  },
  "dependencies": {
    "live-server": "~0.8.2"
  }
}
